<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-4" style="margin-left: 18px;">

<h2><b>esqueceu sua senha?</b></h2>
<p style="font-size: 18px; color:#383838, font-weight: lighter; margin-top: 36px;">No campo abaixo, insira seu email registrado na criação da sua conta.<br><br>
Nesse e-mail, você irá receber instruções para criação de uma nova senha. </p>

<br>

<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email,
    label: 'email',
                required: true,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "ENVIAR" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>

</div>
</div>
</div>
